#include<stdio.h>
main()
{
	int n,i,j,sum=0,f;
	printf("enter how many elements=");
	scanf("%d",&n);
	int a[n];
	for(i=0;i<n;i++)
	{
		scanf("%d",&a[i]);
	}
	for(i=0;i<n;i++)
	{
		f=1;
		while(a[i]>0)
		{
			f=f*a[i];
			a[i]--;
		}
		sum=s+f;
	}
	printf("%d",sum);
}
