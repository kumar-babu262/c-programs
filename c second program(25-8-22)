#include<stdio.h>
#include<string.h>
main()
{
	char ch[100];
	scanf("%s",&ch);
	int i=0;
     int l=strlen(ch);
     for(i=0;i<l;i++)
	{
		if(i%2==1)
		{
			printf("%c",ch[i]-32);
		}
		else
		{
			printf("%c",ch[i]);
		}
	}
}
