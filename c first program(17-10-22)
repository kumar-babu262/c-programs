#include<stdio.h>
main()
{
	int n,i,k;
	scanf("%d",&n);
	int arr[n];
	for(i=0;i<n-1;i++)
	{
		scanf("%d",&arr[i]);
	}
	for(i=0;i<n;i++)
	{
		k=i+1;
		if(arr[i]!=k)
		{
			printf("%d",k);
			break;
		}
	}
	}
	
