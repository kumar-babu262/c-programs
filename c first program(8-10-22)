#include<stdio.h>
main()
{
	int n,x,y,r,sum=0,sum1=0;
	scanf("%d%d%d",&n,&x,&y);
	while(n>0)
	{
		r=n%10;
		if(r==x)
		{
			r=y;
		}
		sum=sum*10+r;
		n=n/10;
	}
	while(sum>0)
	{
		r=sum%10;
		sum1=sum1*10+r;
		sum=sum/10;
	}
	printf("%d",sum1);
}
